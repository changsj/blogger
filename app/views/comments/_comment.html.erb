<div>
  <h4>Comment by <%= comment.author_name %>
  </h4>
  <small>
    Posted <%= distance_of_time_in_words(comment.created_at, Time.now) %> before
  </small>
  <p class="comment"><%= comment.body %></p>
</div>